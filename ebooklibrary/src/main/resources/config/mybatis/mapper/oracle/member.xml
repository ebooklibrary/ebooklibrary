<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.mybatis.mapper.oracle.member">
	<insert id="inserMember" parameterType="MemberVO">
		<selectKey keyProperty="memberNo" resultType="int" order="BEFORE">
			select member_seq.nextval from dual
		</selectKey>
		insert into member(MEMBER_NO,USERNAME,
			PWD,EMAIL1,EMAIL2,TEL1,TEL2,TEL3,BIRTH,GENDER)
		values(#{memberNo},#{username},#{pwd},#{email1},#{email2}
			,#{tel1},#{tel2},#{tel3},#{birth},#{gender})
	</insert>
</mapper>























