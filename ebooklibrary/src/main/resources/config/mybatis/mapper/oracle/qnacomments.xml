<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="config.mybatis.mapper.oracle.qnacomments">
	
	<select id="selectAllComment" parameterType="int" resultType="QnaCommentVO">
		select * from  QNA_COMMENTS where QNA_NO=#{qnaNo} order by  groupNo, sortNo asc
	</select>
	
	<insert id="insertComment" parameterType="QnaCommentVO">
		<selectKey keyProperty="no" resultType="int"
			order="BEFORE">
			select comment_seq.nextval from dual
		</selectKey>
		insert into QNA_COMMENTS(COMMENT_NO, user_name,content,group_no)
		values(#{no},#{userName},#{content},#{qnaNO}); 
	</insert>
	
	<update id="updateSortNo" parameterType="QnaCommentVO">
		update QNA_COMMENTS
		set sortno = sortno+1
		where groupno=#{groupNo} and sortNo > #{sortNo} 
	</update>
	
	<insert id="commentReply" parameterType="QnaCommentVO" >
		<selectKey keyProperty="no" resultType="int"
			order="BEFORE">
			select QNA_COMMENTS.nextval from dual
		</selectKey>
		insert into QNA_COMMENTS(COMMENT_NO, user_name,
			content,qna_no, group_no, step_no, sort_No )
		values(#{no} ,#{userName},#{content},#{qnaNo}
			#{groupNo}, #{stepNo}, #{sortNo})
	</insert>
	
	
	
</mapper>
